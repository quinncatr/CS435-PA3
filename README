Apache Spark is sparkConfig and is already on your machine from PA0.
Commands:
start-master.sh
start-workers.sh
stop-all.sh

Git Commands:
Committing:
git add .
git commit -m “message”
git push (remote repository) (branch name)
origin is the shorthand 
ex: git push origin main (if pushing to main branch)

Switch Branches:
git fetch origin
git checkout 3-add-dataset
