Apache Spark is sparkConfig and is already on your machine from PA0.
Commands:
start-master.sh
start-workers.sh
stop-all.sh

Git Commands:
Committing:
git add .
git commit -m “message”
git push (remote repository) (branch name)
origin is the shorthand 
ex: git push origin main (if pushing to main branch)

Switch Branches:
git fetch origin
git checkout 3-add-dataset

Hadoop run commands(I put all the .java files in a folder named pagerank):
javac -cp "$(hadoop classpath):$SPARK_HOME/jars/*:." pagerank/*
jar -cvf PageRank.jar pagerank
JOB COMMAND:
spark-submit \
  --class pagerank.PageRank \
  --master yarn \
  PageRank.jar \
  hdfs:///PA3/Demo-Dataset/links-simple-sorted-sample.txt \
  hdfs:///PA3/Demo-Dataset/titles-sorted-sample.txt \
  /PA3/output-ideal \
  /PA3/output-taxed
